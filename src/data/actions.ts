export const enum ActionType {
  // todo: convert these to a const enum once rollup correctly outputs them
  LOAD_TARGET = 0,
  SET_TARGET_STATE = 1,
  UPDATE_TARGET_PROPS = 2,
  UPDATE_TARGET_STATES = 3,
  UPDATE_TARGET_TARGETS = 4,
  TRANSITION_TARGET_STATE = 5
}

